<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="
             http://maven.apache.org/POM/4.0.0
             https://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>kit.sdq.kastel.vitruvius</groupId>
        <artifactId>case-study</artifactId>
        <version>0.1.0-SNAPSHOT</version>
        <relativePath>../pom.xml</relativePath>
    </parent>

    <artifactId>case-study-consistency</artifactId>
    <packaging>jar</packaging>

    <name>F1 Race Car Consistency</name>

    <dependencies>
        <!-- Generated EMF model classes -->
        <dependency>
            <groupId>${project.groupId}</groupId>
            <artifactId>case-study-model</artifactId>
            <version>${project.version}</version>
        </dependency>

        <!-- Vitruvius -->
        <dependency>
            <groupId>tools.vitruv</groupId>
            <artifactId>tools.vitruv.change.composite</artifactId>
        </dependency>

        <dependency>
            <groupId>tools.vitruv</groupId>
            <artifactId>tools.vitruv.change.propagation</artifactId>
        </dependency>

        <dependency>
            <groupId>tools.vitruv</groupId>
            <artifactId>tools.vitruv.dsls.reactions.runtime</artifactId>
        </dependency>

        <dependency>
            <groupId>org.eclipse.emf</groupId>
            <artifactId>org.eclipse.emf.ecore</artifactId>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <!-- Adds Reactions and generated Java code as source folders -->
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>build-helper-maven-plugin</artifactId>

                <executions>
                    <execution>
                        <id>add-source-reactions</id>
                        <phase>generate-sources</phase>

                        <goals>
                            <goal>add-source</goal>
                        </goals>

                        <configuration>
                            <sources>
                                <source>
                                    ${project.basedir}/src/main/reactions
                                </source>
                                <source>
                                    ${project.build.directory}/generated-sources/reactions
                                </source>
                            </sources>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

            <!-- Compiles the Reactions DSL -->
            <plugin>
                <groupId>org.eclipse.xtext</groupId>
                <artifactId>xtext-maven-plugin</artifactId>

                <configuration>
                    <languages>
                        <language>
                            <setup>
                                tools.vitruv.dsls.reactions.ReactionsLanguageStandaloneSetup
                            </setup>

                            <outputConfigurations>
                                <outputConfiguration>
                                    <outputDirectory>
                                        ${project.build.directory}/generated-sources/reactions
                                    </outputDirectory>
                                </outputConfiguration>
                            </outputConfigurations>
                        </language>

                        <language>
                            <setup>
                                org.eclipse.xtend.core.XtendStandaloneSetup
                            </setup>

                            <outputConfigurations>
                                <outputConfiguration>
                                    <outputDirectory>
                                        ${project.build.directory}/generated-sources/xtend
                                    </outputDirectory>
                                </outputConfiguration>
                            </outputConfigurations>
                        </language>
                    </languages>
                </configuration>

                <dependencies>
                    <dependency>
                        <groupId>tools.vitruv</groupId>
                        <artifactId>tools.vitruv.dsls.reactions.language</artifactId>
                        <version>${vitruv.version}</version>
                    </dependency>

                    <dependency>
                        <groupId>tools.vitruv</groupId>
                        <artifactId>tools.vitruv.dsls.reactions.runtime</artifactId>
                        <version>${vitruv.version}</version>
                    </dependency>

                    <dependency>
                        <groupId>tools.vitruv</groupId>
                        <artifactId>tools.vitruv.dsls.common</artifactId>
                        <version>${vitruv.version}</version>
                    </dependency>

                    <!-- Makes generated EPackages available to Reactions -->
                    <dependency>
                        <groupId>${project.groupId}</groupId>
                        <artifactId>case-study-model</artifactId>
                        <version>${project.version}</version>
                    </dependency>
                </dependencies>
            </plugin>
        </plugins>
    </build>

</project>